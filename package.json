{
  "name": "taskflow",
  "version": "1.0.0",
  "scripts": {
    "start": "cd server && node src/app.js",
    "install:server": "cd server && npm install"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}